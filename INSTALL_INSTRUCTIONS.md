# دليل تثبيت تطبيق تراوف (Android & iOS)

تم تحويل الموقع بنجاح إلى تطبيق PWA متكامل، بالإضافة إلى تجهيز ملفات بناء تطبيقات أصلية (Native Apps).

## 1️⃣ الطريقة الأسرع: التثبيت المباشر (PWA)
يعمل التطبيق الآن كتطبيق أصلي على جوالك بدون الحاجة لمتجر التطبيقات.

### 📱 على Android (Chrome)
1. افتح رابط الموقع في متصفح Chrome.
2. اضغط على الثلاث نقاط (القائمة) ⠇.
3. اختر **"تثبيت التطبيق" (Install App)** أو **"الإضافة إلى الشاشة الرئيسية"**.
4. سيظهر التطبيق في قائمة تطبيقاتك مع الأيقونة الخاصة وبكامل الشاشة.

### 🍎 على iOS (Safari)
1. افتح رابط الموقع في متصفح Safari.
2. اضغط على زر المشاركة (Share) 📤 في الأسفل.
3. اختر **"إضافة إلى الشاشة الرئيسية" (Add to Home Screen)**.
4. اضغط **"إضافة" (Add)**.

---

## 2️⃣ بناء تطبيق APK/IPA (للمطورين)
لقد تم إنشاء ملف `capacitor.config.json` و `package.json` لتتمكن من بناء نسخة أصلية (Native) إذا كان لديك بيئة تطوير (Node.js + Android Studio / Xcode).

### الخطوات:
1. حمل الملفات على جهاز الكمبيوتر.
2. افتح التيرمينال ونفذ الأمر التالي لتثبيت المكتبات:
   ```bash
   npm install
   ```
3. لإضافة دعم Android:
   ```bash
   npx cap add android
   ```
4. لفتح المشروع في Android Studio وبناء APK:
   ```bash
   npx cap open android
   ```
   *من داخل Android Studio: اذهب إلى Build > Build Bundle(s) / APK(s) > Build APK.*

5. لإضافة دعم iOS (يتطلب جهاز Mac):
   ```bash
   npx cap add ios
   npx cap open ios
   ```

### ملاحظات هامة:
- التحديثات: التطبيق مصمم لتحميل المحتوى مباشرة من GitHub (`https://81fr.github.io/Trao-System0/`)، لذا أي تحديث ترفعه على الموقع سيظهر في التطبيق فوراً دون الحاجة لإعادة بناء التطبيق!
- الأيقونات: تأكد من وجود شعار بمقاسات مختلفة للحصول على أفضل جودة.
